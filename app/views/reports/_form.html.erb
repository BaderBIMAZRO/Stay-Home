<%= bootstrap_form_with(model: @report, local: true) do |form| %>

      <% if @report.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@report.errors.count, "error") %> prohibited this report from being saved:</h2>

      <ul>
      <% @report.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
<div class="container">
<form>
    <div>
   <%= form.text_field :title, label:"Issue" %>
  </div>
<div>
    <%= form.text_field :description, label:"Description" %>
</div>
    <div class="actions">
    <%= form.submit %>
   <button> <%= link_to 'Back', reports_path %>  </button>
  </div>
</form>
</div>
  <% end %>